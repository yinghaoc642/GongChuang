# 竞赛满分基线说明

版本日期：2026-07-30

## 版本定位

本工作树已经完成“代码侧 P0 基线”重构：双启停区、严格任务码、两批搬运、
同色码垛、Vision v5、协议 v2、分层超时和自动预检均已接入并通过构建。

它仍不能被表述为“实车满分闭环版”。当前底盘 X/Y 主要依靠命令脉冲推算，
`FINAL_ALIGN` 只检查名义终点外廓；没有硬件协议时，软件无法证明车体真实
进入启停区，也无法证明抓放成功或绕过随机障碍。

评分审计显示放置相关项目约占原始分的 180/211。因而真实定位、障碍感知和
抓放确认不是锦上添花，而是决定满分上限的核心输入。

## 本版完成项

- 启停区 1/2 共用一套参数化路线：
  - Start1/Start2 均到达名义扫码点 `(2215,1200)`；
  - 名义终点分别为 `(2250,2250)`、`(2250,150)`；
  - 230×300 mm 名义车体外廓完整位于对应 300×300 mm 区域。
- PB9 操作：
  - 启动前双击：切换 Zone1/Zone2；
  - 单击：锁定当前选择并启动；
  - 运行中长按：软件急停并进入不可直接重启的故障状态。
- 二维码：
  - 15 字节结构、分隔符、颜色集合、颜色重复、位置排列严格校验；
  - 一帧通过严格格式校验的有效任务码即锁定任务；
  - 两个启停区使用对称扫码前探方向；
  - 搜索超时与扫码成功后的安全回位超时分离。
- 原料：
  - 任务内任一尚未抓取颜色可先抓，但必须放入该颜色对应的二维码槽位，
    并通过槽位掩码防止重复抓取；
  - Vision 端稳定后，STM32 再要求两次独立坐标确认；
  - 任务外颜色和已填槽位颜色只触发重试，不会进入抓取流程。
- 圆环：
  - 必须先找到近似共线、等间距、等半径的三圆整体；
  - 明确返回中间 `target=2`，不再把最近任意圆冒充 2 号环；
  - 视觉累计修正限制为 220 mm、最多 6 次。
- VisionProtocol v2：
  - 6 字节请求帧包含版本、请求序号和 CRC-8；
  - 响应包含序号、模式、状态、目标、坐标、质量、置信度、时间戳和 CRC；
  - 主控拒绝 CRC 错误、过期序号、模式串台、错误目标和越界字段；
  - 超长响应整帧丢弃，不解析残余尾段。
- 安全监督：
  - 底盘 20 s、转向 16 s；
  - QR 搜索 20 s；
  - 视觉单次 12 s，最多额外重试 2 次；
  - RAW/PROCESS/STORAGE 总时限 45/65/45 s；
  - 任务进度看门狗 45 s；
  - 当前调试版关闭整场 180 s 硬停；正式比赛可将
    `ENABLE_COMPETITION_TIME_LIMIT` 改回 `true`；
  - 运行中不再执行阻塞式舵机 `ping`。
- M6/M7 与端点定位：
  - M6 驱动器和固件必须同时为 256 细分；M7 保持 16 细分，两轴独立换算；
  - 上电先用 0.5 mm 低速探测并核对编码器角度，通过后才补足 10 mm
    工作零偏置，可在长伸出前拦截细分或方向错配；
  - 没有限位开关，故障/复位后必须先人工把 M6 完全缩回、M7推到最高点；
  - 端点采用 M5 到位消振后再动 M6，初始角为 1号 `+20°`、3号 `-20°`，
    无结果时只尝试同侧 `±30°`、`±10°`；
  - `3A` 堵转状态必须连续 3 帧才确认，健康帧清零；确认后最多一次降档
    位置核验恢复，`FD E2` 不再直接冒充堵转。
- 纯逻辑模块：
  - `CompetitionCore`：任务码、同色码垛、启停区和外廓；
  - `VisionProtocol`：请求构帧、CRC 和严格响应解析。

## 必须成套部署

STM32 固件和 MaixCAM 脚本协议不向后兼容，必须同时更新：

- STM32入口：`src/main.cpp`
- STM32共享实现：`src/yanyanversionnew.inc`
- MaixCAM：`vision/yanyanview5.py`
- 协议：`vision/protocol.md`

旧 Vision v4 的三字节请求不会被本版主控接受。本版请求中的协议版本本身也是
运行时兼容性门禁：烧错相机脚本会无结果并在有限时间内故障，不会静默使用
未知格式坐标。

## 已通过验证

```powershell
& "$env:USERPROFILE\.platformio\penv\Scripts\platformio.exe" run
python test\route_preflight.py
python -m py_compile vision\yanyanview5.py
python test\vision_logic_test.py
```

另有两个无 Arduino 依赖的 C++ 原生测试：

- `test/native_competition_core.cpp`
- `test/native_vision_protocol.cpp`

当前 STM32 构建资源：

- Flash：约 89.2 KB / 128 KB；
- RAM：约 7.4 KB / 512 KB。

## 满分闭环仍需的硬件输入

请在继续开发前提供以下确切信息；仅给器件名称不够，还需要协议、刷新率、
安装方向和实测范围。

1. X/Y 定位：OPS9、轮编码器、光流或其他定位器的型号、接口和帧格式。
2. 障碍物：雷达、ToF 或视觉方案，安装坐标、盲区、量程和数据协议。
3. 终点确认：向下红外、边线相机或其他四角入区传感器的引脚与有效电平。
4. 机械臂寻零：M5/M6/M7 限位开关、驱动器碰撞回零参数及安全方向。
5. 抓放确认：夹爪负载、物料存在和载物盘三槽检测方式。
6. 电源：电池标称电压、ADC 分压比、欠压告警和硬停阈值。
7. MaixCAM：固件版本、曝光 API、现场原图，以及三圆是否能同时进入视野。
8. 终点外廓：收臂后的实测 40/75 mm 争议尺寸和完整车体包络。
9. HMI：页面工程和控件表，用于显示启停区、剩余时间、协议版本和故障码。

## 实车放行门槛

- `vision/calibration.json` 的 `verified` 仍为 `false`，不得仅改标志绕过验收。
- 确认扫码器能稳定输出一帧完整的 15 字节任务码，并用串口噪声、截断帧和
  超长帧验证只有通过严格格式校验的有效帧才会锁定任务。
- 用比赛物料和现场光照重新标定四色阈值、曝光和像素变换。
- 至少执行 100 组三圆测试，记录错号、误检、漏检和最坏延迟。
- 分别对 Zone1/Zone2 做满载、低电量和最大视觉重试的 180 s 全流程测试。
- 在接入真实 X/Y、障碍和终点传感后，才可把 `FINAL_ALIGN` 从名义门禁改成
  实测闭环完成条件。
