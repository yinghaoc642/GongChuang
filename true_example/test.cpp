/*
 * 粗加工区视觉定位 + 三物料放置测量模式。
 *
 * 使用方法：
 *   1. 保留 src/yanyanversionnew.inc；
 *   2. 本文件就是 PlatformIO 的烧录入口，不需要再复制别的 C++；
 *   3. 同时给 MaixCAM 部署 vision/yanyanview5.py
 *      （版本5.3.0-find1,3，必须支持协议模式10）；
 *   4. 上电前人工确认M5旧坐标0°、M6抵住回缩端、M7位于物理最高点；
 *      上电后程序自动令M6向外、M7向下各移动10 mm并设为工作零点；
 *   5. 料盘槽 0/1/2 各装一个物料，车按正式朝向放在粗加工三圆前；
 *   6. 上电初始化完成后单击 PB9。
 *
 * 程序只执行：
 *   底盘IMU锁向并冻结 -> 机械臂看1号真实圆心 -> 看3号真实圆心
 *   -> M5/M6在当前姿态原地压中，不回-80°重复大转
 *   -> 粗居中后再低头15 mm，连续两次进入中心2 px才记录
 *   -> 两端中点计算2号 -> 对每个圆分别用atan2/hypot计算M5/M6
 *   -> 槽0/1/2下降到物理155 mm，最后15 mm轻微减速
 *   -> 机械臂和料盘回行驶姿态 -> 停在MEASURE。
 * 不扫码、不跑路线、不把三个物料重新抓回。
 *
 * 圆内印刷的数字 1/2/3 必须保留，它们是比赛真实视觉条件。
 */
#define GONGCHUANG_VISION_YANYAN_TEST 1
#include "../src/yanyanversionnew.inc"
